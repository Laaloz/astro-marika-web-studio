---
// Renders portable text blocks with the custom `priceNote` style
// (see twoColumnContentSectionType.ts) as a small muted line, e.g. "Paketit alkaen 900 €".
---

<p class="price-note"><slot /></p>
